# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# --- Data validation / settings ---
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0

# --- Database ---
SQLAlchemy==2.0.36
asyncpg==0.30.0
psycopg2-binary==2.9.10
alembic==1.14.0
greenlet==3.1.1

# --- Security ---
passlib[bcrypt]==1.7.4
# Pinned below 4.1: passlib 1.7.4 reads bcrypt.__about__.__version__ during
# backend detection, which bcrypt 4.1 removed. The failure is trapped by
# passlib so hashing still works, but it logs an AttributeError traceback on
# every process start. 4.0.1 keeps that attribute and silences the noise.
bcrypt==4.0.1
PyJWT==2.10.1
cryptography==44.0.0

# --- HTTP client for monitoring checks ---
# socks: SOCKS5 proxy support, which is how a vantage-point check leaves this
# host by a different route (a Tor SocksPort, or a VPN container exposing one).
httpx[http2,socks]==0.28.1

# --- Caching / coordination ---
redis==5.2.1

# --- Bulk import/export ---
openpyxl==3.1.5

# --- Observability ---
structlog==24.4.0

# --- Rate limiting ---
limits==3.14.1

# --- Testing ---
pytest==8.3.4
pytest-asyncio==0.25.0
aiosqlite==0.20.0
respx==0.22.0

# --- Page captures ---
# Renders a monitored page to a JPEG for the "last success / last failure"
# screenshots. Optional at runtime: app/monitoring/screenshot.py imports it
# lazily and records a reason rather than failing when it or its Chromium is
# absent, so a slimmed image still runs - it just never produces screenshots.
playwright==1.49.1
